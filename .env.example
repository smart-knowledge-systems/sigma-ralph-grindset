# Anthropic API key (required for --api and --batch modes)
ANTHROPIC_API_KEY=

# Override the project root directory (optional, auto-detected in portable mode)
# SIGMA_PROJECT_ROOT=

# Log level: debug, info, warn, error (default: info)
# LOG_LEVEL=info
